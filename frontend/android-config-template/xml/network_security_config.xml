<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite tráfego HTTP puro (sem TLS) para o Atlas conversar com o backend
  local (127.0.0.1) ou com outra máquina na rede local (192.168.x.x, 10.x.x.x).
  Sem isso, o Android bloqueia HTTP cleartext por padrão a partir da API 28.

  Depois de rodar `npx cap add android`, copie este arquivo para:
    android/app/src/main/res/xml/network_security_config.xml
  E garanta que o AndroidManifest.xml (android/app/src/main/AndroidManifest.xml)
  tem, na tag <application>:
    android:networkSecurityConfig="@xml/network_security_config"
    android:usesCleartextTraffic="true"
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
